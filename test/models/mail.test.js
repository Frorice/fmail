const should = require('should');
const mongoose = require('mongoose');
const Mail = require ('/lib/models/mail.js');

mongoose.connect('mongodb://127.0.0.1/test');

describe('test/models/mail.test.js',function (){
  var mail = new Mail({
    
  });
  it('')
});